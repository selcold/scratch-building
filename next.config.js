/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['uploads.scratch.mit.edu', 'cdn.discordapp.com', 'scratchblocks.github.io'],
    },
}

module.exports = nextConfig